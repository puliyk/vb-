代码片段智能说明书生成器 AI 开发指令
项目概述
这是一个纯前端的代码片段智能说明书生成器，使用 HTML5 + CSS3 + 原生 JavaScript (ES6) 开发。用户粘贴 Java 或 Python 代码片段后，系统自动生成技术版说明书（函数签名、参数表格、返回值、依赖、异常处理）和小白版说明书（生活比喻、使用场景、输入输出）。所有解析均在浏览器本地完成，不上传任何代码，不调用任何外部 AI 接口。

开发规范
使用原生 HTML/CSS/JS，不使用 React、Vue、Angular 等框架

样式使用 CSS Grid + Flexbox 实现响应式布局，不使用 Tailwind 或 UI 库

所有代码可内联在一个 index.html 文件中（也可拆分为 CSS/JS 文件）

解析引擎基于正则表达式，硬编码生活比喻库（至少 20 条，最终 38 条）

语法高亮使用 Prism.js（CDN 引入）

导出分享卡片使用 html2canvas（CDN 引入）

无后端、无数据库、无 LocalStorage，所有数据仅在内存中处理

代码风格
使用原生 JavaScript ES6 语法（let、const、箭头函数、模板字符串等）

函数名使用 camelCase（如 parseJava、renderTechDoc）

常量使用 UPPER_SNAKE_CASE（如 METAPHOR_LIBRARY、DEFAULT_METAPHOR）

变量名清晰语义化，避免单字母变量

关键正则表达式添加注释说明

模块化：解析器、渲染器、比喻库分离为独立函数

测试要求
每个功能完成后手动测试（粘贴样例代码，验证输出）

测试边界情况：

空代码输入

非函数代码（如纯变量声明）

匿名函数/箭头函数

复杂嵌套代码

语言切换是否正确

验收标准七条全部自测通过（见项目计划书）

准备三个演示样例（Java、Python、匿名函数）

注意事项
保持代码简洁，避免过度设计

优先实现核心功能（第7-10周：骨架、Java解析、Python解析、双版说明书）

确保移动端适配（宽度 < 768px 时双栏自动变为上下堆叠）

不上传任何用户代码，不调用任何外部服务

不使用 eval 或 new Function 执行用户代码，保证安全

加载动画体验流畅，解析时按钮防抖

错误提示友好、具体（如“请粘贴一段 Java 或 Python 函数代码”）

