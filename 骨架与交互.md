角色与任务
你是一位资深前端交互专家。现在我们要开发一个名为“代码片段智能说明书生成器”的纯前端Web应用。请帮我实现第7-8周的页面骨架和交互基础。

项目约束（必须遵守）
纯前端实现，所有代码内联在一个 index.html 文件中（HTML+CSS+JS）。
不使用任何前端框架（如React/Vue），使用原生HTML/CSS/JS。
不调用任何外部AI接口。
本周具体要求
布局：使用 CSS Grid 或 Flexbox 实现左右双栏布局。左侧为代码输入区，右侧为说明书展示区。在屏幕宽度 <768px 时，自动变为上下堆叠。
顶部导航：包含项目名称，以及一个 Java / Python 的下拉切换菜单。
左侧输入区：一个 textarea 用于粘贴代码，下方有一个“生成说明书”按钮。
右侧展示区：包含两个标签页（技术版 / 小白版），默认展示技术版空白状态。
交互逻辑：
切换顶部语言时，在控制台打印当前选择的语言（如 “Switched to Python”）。
点击“生成说明书”按钮时，暂不实现解析，弹窗提示“解析引擎开发中”。
视觉风格：要求现代、干净、高端。使用深色主题（类似VS Code），卡片带有微弱的阴影和圆角，按钮有hover过渡效果。
输出要求
请直接输出完整的 index.html 代码，不要省略。