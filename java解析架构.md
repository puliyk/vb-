任务：Java/JS 解析引擎 + 技术版说明书（严谨版）
基于极简设计系统，实现解析引擎。本周的核心是建立“事实与推论分离”的架构。

具体要求：

parseJava(code) 数据结构升级：
提取事实：函数名、明确的参数及类型、import、try-catch。
推演逻辑：如果返回值类型未在代码中明确声明，需通过return语句推断，并在数据结构中标记 isInferred: true 及 inferReason。
技术版排版（展现严谨性）：
函数签名区：已验证的事实用常规字体，推论出的类型用斜体+虚线下划线，鼠标悬停时出现极简的Tooltip：“AI推断：基于return语句”。
新增【洞察】模块：在签名下方，用一行次要颜色的文本展示对该函数设计意图的深刻理解。
参数列表：摒弃粗糙表格，使用结构化排版，参数名为代码字体（主题色），类型为次要字体。
空状态与错误：解析失败时，不要粗暴报错，而是展示一句带有哲学意味的提示：“代码的意图尚不明朗，请检查输入”。
请输出完整的 index.html。